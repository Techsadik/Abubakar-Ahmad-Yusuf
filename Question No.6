fun main() {
    // Create an array of names
    val names = arrayOf("Alice", "Bob", "Charlie", "Diana", "Ethan")

    // Loop through the array and greet each name
    for (name in names) {
        println("Hello, $name")
    }
}
